<%# Dados a serem processados ao inicio da página %>
<% if flash[:notice] %>
    <div class="alert alert-success" role="alert">
        <%= flash[:notice] %>
    </div>
<% end %>

<%# Corpo do meu formulário de exibição%>
<h3>Todos os Produtos</h3>
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <td>Nome</td>
            <td>Descriçao</td>
            <td>Preço</td>
            <td>Quantidade</td>
            <td>Departamento</td>
            <td colspan="2">
            
        </tr>
    </thead>

    <tbody>
        <%# todos = Produto.all %>
        <%= render @produtos %>
    </tbody>
</table>

<div class="alert alert-success" role="alert" >
    <h4 class="alert-heading" >Produto com Desconto</h4>
</div>
<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <td>Nome</td>
            <td>Descriçao</td>
            <td>Preço</td>
            <td>Quantidade</td>
            <td>Departamento</td>
            <td colspan="2">
        </tr>
    </thead>

    <tbody>
        <%# todos = Produto.all %>
        <%= render @produto_com_desconto %>
    </tbody>
</table>